services:
  bot:
    container_name: clockin-bot
    user: "1000:1000"
    build: .
    env_file:
      - .env
    restart: unless-stopped
    environment:
      DATA_DIR: /data
    ports:
      - "${PORT:-3000}:${PORT:-3000}"
    volumes:
      - ./data:/data
