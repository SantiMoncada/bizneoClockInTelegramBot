services:
  bot:
    build:
      context: .
      dockerfile: Dockerfile
    container_name: bizneo-clockin-bot
    restart: unless-stopped
    env_file:
      - .env
    environment:
      DATA_DIR: /data
      NODE_ENV: production
    volumes:
      - ./data:/data
